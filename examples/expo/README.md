# Expo TypeScript example

This example demonstrates `@raulpesilva/re-state` with Expo SDK 57.

## Run

Install the example in isolation and start Expo:

```sh
pnpm install --ignore-workspace --lockfile=false
pnpm start
```

The example keeps the published `@raulpesilva/re-state` dependency range used by the other examples.
