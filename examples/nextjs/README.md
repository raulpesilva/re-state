# Next.js TypeScript example

This example uses the Next.js App Router with React 19 and demonstrates re-state in a client component.

Install the dependencies and start the development server:

```sh
pnpm install
pnpm dev
```

Run the local checks with `pnpm format:check`, `pnpm lint`, `pnpm typecheck`, and `pnpm build`.
